# Test my first streamlit
#### before run main.py, please install streamlit with command:
```
pip install streamlit
```

#### then run this command to deploy application with command:
```
streamlit run main.py
```

###Thanks:)
